Setup Notes
-----------

Add:
 - add to your .bashrc:
	export PATH=$PATH:~/sutro
 - create /var/www/html/lapse and make it writable by the user

add to crontab -e:

* * * * * /home/pi/sutro/snapshot.sh 2>&1
* * * * * sleep 30;/home/pi/sutro/snapshot.sh 2>&1


