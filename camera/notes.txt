Setup Notes
-----------

Add:
 - add to your .bashrc:
	export PATH=$PATH:~/sutro
 - create /var/www/html/lapse and make it writable by the user

add to crontab -e:

* * * * * sutro/camera/lapse.sh >> lapse/lapse.log 2>&1

